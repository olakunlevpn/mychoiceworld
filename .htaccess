<IfModule mod_rewrite.c>
    RewriteEngine On
    # Deny all direct access to the app directory
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Block direct access to sensitive files
<FilesMatch "\.(env|log|json|lock|md|sh)$">
    Order allow,deny
    Deny from all
</FilesMatch>
